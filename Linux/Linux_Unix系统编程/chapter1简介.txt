chapter1简介
1.1 概述

1.2一个简单的时间获取客户程序
	网际套接字地址结构中IP地址和端口号这两个成员必须使用特定的格式，为此我们调用库函数htons("主机到网络端整数")去转换二进制端口号，又调用库函数inet_pton("呈现形式到数值")去把ASCII命令行参数转换为合适的格式。

1.3协议无关性

1.4 错误处理：包裹函数

1.5 一个简单的时间获取服务器程序
	1：创建TCP套接字
	2：吧服务器的众所周知端口捆绑到套接字
	3：把套接字转换成监听套接字
	4：接受客户端连接，发送应答
		snprinf(buff, sizeof(buff), "%.24s\r\n", ctime(&ticks));
		snprinf相比sprintf而言，sprinf无法检查目的缓冲区是否溢出。
	5：终止连接

1.6 

1.7 OSI模型

1.8 BSD网络支持历史
	(1):netstat -i 提供网络接口的信息
	(2):netstat -r 展示路由表
	(3):ifconfig etho
	(4):ping